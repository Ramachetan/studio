export const DEFAULT_PROMPT = `You are an expert receipt parser. You will extract the items, quantities, prices, tax, and total from the receipt.

Return the data in JSON format.

Receipt: {{media url=receiptDataUri}}`;
